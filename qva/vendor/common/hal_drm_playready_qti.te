#Copyright (c) 2023 Qualcomm Innovation Center, Inc. All rights reserved.
#SPDX-License-Identifier: BSD-3-Clause-Clear
#

type vendor_hal_drm_playready_qti, domain;
hal_server_domain(vendor_hal_drm_playready_qti, hal_drm)

type vendor_hal_drm_playready_qti_exec, exec_type, vendor_file_type, file_type;
init_daemon_domain(vendor_hal_drm_playready_qti)

allow vendor_hal_drm_playready_qti mediacodec:fd use;
allow vendor_hal_drm_playready_qti { appdomain -isolated_app_all }:fd use;

vndbinder_use(vendor_hal_drm_playready_qti);

allow vendor_hal_drm_playready_qti vendor_qdisplay_service:service_manager { find };

allow vendor_hal_drm_playready_qti vendor_mediadrm_vendor_data_file:dir create_dir_perms;
allow vendor_hal_drm_playready_qti vendor_mediadrm_vendor_data_file:file create_file_perms;
allow vendor_hal_drm_playready_qti vendor_dmabuf_qseecom_heap_device:chr_file r_file_perms;
